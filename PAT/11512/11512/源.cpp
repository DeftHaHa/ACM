#include<iostream>
#include<cstdio>
#include<cstring>
using namespace std;
int ino[1005], pre[1005];
int M, N;
int u, v;
int main()
{
	scanf_s("%d%d", &M, &N);
	for (int i = 0; i < N; i++) scanf_s("%d", &ino[i]);
	for (int i = 0; i < N; i++) scanf_s("%d", &pre[i]);
	while (N--)
	{
		scanf_s("%d%d", &u, &v);

	}
	return 0;
}