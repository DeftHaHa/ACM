#include<stdio.h>
#include<math.h>
int main()
{
        long long n;
        while(1)
        {
                scanf("%lld",&n);
                printf("%lld\n",llabs(n));
        }
        return 0;
}
