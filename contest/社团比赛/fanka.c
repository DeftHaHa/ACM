#include<stdio.h>
int main()
{
        int n;
        int price[50];
        int last;
        int i;
        scanf("%d",&n);
        for(i=0;i<n;i++)
                scanf("%d",&price[i]);
        scanf("%d",&last);
        if(last==5) {printf("0\n");return 0;}
        else
        {


        }

        return 0;
}
